// Create a function that meets the following:
// Function's name - getDouble
// Function's purpose - return double of a given value
// Input - given value
// Output - the input x 2
var numToDouble = prompt('Enter a number and I\'ll double it');
function getDouble(numToDouble) {
    return (numToDouble * 2);
}
console.log(getDouble(numToDouble));
